<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="shellXamarin.Module.Home.Views.HomePage" xmlns:translate="clr-namespace:shellXamarin.Module.Home.i18n" Title="{translate:Translate home.title}">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <ActivityIndicator Grid.Row="0" IsRunning="{Binding IsBusy}" WidthRequest="80" HeightRequest="80" />
            <Button Grid.Row="1" Text="{translate:Translate home.action.navigation.settings}" Command="{Binding NavigateToSettingsPageCommand}" Margin="20" BackgroundColor="#da1f26" TextColor="White" VerticalOptions="Center"></Button>
            <Button Grid.Row="2" Text="{translate:Translate home.action.navigation.login}" Command="{Binding NavigateToLoginPageCommand}" Margin="20" BackgroundColor="#da1f26" TextColor="White" VerticalOptions="Center"></Button>
        </Grid>
    </ContentPage.Content>
</ContentPage>
