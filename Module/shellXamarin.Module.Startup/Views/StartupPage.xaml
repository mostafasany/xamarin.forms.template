<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="shellXamarin.Module.Startup.Views.StartupPage" xmlns:translate="clr-namespace:shellXamarin.Module.Startup.i18n" xmlns:theme="clr-namespace:shellXamarin.Module.Common.Themes;assembly=shellXamarin.Module.Common">
    <ContentPage.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <theme:MainTheme></theme:MainTheme>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <ActivityIndicator Grid.Row="0" IsRunning="{Binding IsBusy}" Color="{DynamicResource SpinnerColor}" WidthRequest="80" HeightRequest="80" />
            <Button Grid.Row="1" Text="{translate:Translate startup.action.proceed}" Command="{Binding NavigateLoginPageCommand}" Margin="20" BackgroundColor="{DynamicResource PrimaryButtonColor}" TextColor="{DynamicResource BaseTextColor}" VerticalOptions="Center"></Button>
        </Grid>
    </ContentPage.Content>
</ContentPage>