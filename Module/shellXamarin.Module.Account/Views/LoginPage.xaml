<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" FlowDirection="{Binding FlowDirection}" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="shellXamarin.Module.Account.Views.LoginPage" xmlns:translate="clr-namespace:shellXamarin.Module.Account.i18n" Title="{translate:Translate account.login.title}" xmlns:theme="clr-namespace:shellXamarin.Module.Common.Themes;assembly=shellXamarin.Module.Common" xmlns:styles="clr-namespace:shellXamarin.Module.Common.Styles;assembly=shellXamarin.Module.Common">
    <ContentPage.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <theme:MainTheme></theme:MainTheme>
                <styles:AppStyles></styles:AppStyles>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid Style="{StaticResource RootLayoutStyle}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <StackLayout Orientation="Vertical" Grid.Row="0">
                <Entry Keyboard="{Binding Email.Keyboard}" Style="{StaticResource FormEntryStyle}" ReturnType="{Binding Email.ReturnType}" Placeholder="{Binding Email.Placeholder}" Text="{Binding Email.Text,Mode=TwoWay}"></Entry>

                <Entry IsPassword="{Binding Password.IsPassword}" Style="{StaticResource FormEntryStyle}" ReturnType="{Binding Email.ReturnType}" Placeholder="{Binding Password.Placeholder}" Text="{Binding Password.Text,Mode=TwoWay}"></Entry>
            </StackLayout>
            <ActivityIndicator Grid.Row="1" IsRunning="{Binding IsBusy}" Style="{StaticResource ActivityIndicatorStyle}" />

            <Button Grid.Row="2" Text="{translate:Translate account.action.login}" Command="{Binding LoginCommand}" Style="{StaticResource PrimaryButtonStyle}"></Button>
            <Button Grid.Row="3" Text="{translate:Translate account.action.back}" Command="{Binding BackCommand}" Style="{StaticResource SecondaryButtonStyle}"></Button>
        </Grid>
    </ContentPage.Content>
</ContentPage>
